<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.actividadaprendizaje">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActividadAprendizaje">
        <activity android:name=".Vista.PeliculasTopRatedVista"></activity>
        <activity android:name=".Vista.LoginVista" />
        <activity android:name=".Vista.PeliculasFiltroGeneroVista" />
        <activity android:name=".Vista.PeliculaFichaDescriptivaVista" />
        <activity android:name=".Vista.PeliculasVista">

            <!-- Se le dice que el launcher arranque con la lista de pelis -->
            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".Vista.InicioVista">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>